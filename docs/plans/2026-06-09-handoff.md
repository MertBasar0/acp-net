# 2026-06-09 Handoff

Bu dosya, 2026-06-09 calismasinin sonunda Acp.Net / OpenClaw subagent runtime hattinin durumunu ozetler.

## Bugun Tamamlananlar

Tamamlanan spike'lar:

1. Spike 008: Environment Policy + OpenClaw Failure Contract.
2. Spike 009: OpenClaw-oriented run artifact.
3. Spike 010: Minimal OpenClaw-style subagent runner sample.

## Kod Durumu

Yeni/degisen ana API:

- `AcpRequiredExecutable`
- `AcpMissingExecutablePolicy`
- `AcpPreflightException`
- `AcpRunFailureKind`
- `AcpRunArtifact`
- `AcpProcessOptions.AgentName`
- `AcpProcessOptions.RunArtifactPath`
- `AcpProcessOptions.RequiredTools`

Geriye uyumluluk:

- `RequiredExecutables = ["tool"]` hala calisir.
- Bu eski API warning davranisini temsil eder.

## Yeni Sample

OpenClaw-style sample:

`src/samples/openclaw-subagent-runner/`

Bu sample fake ACP agent kullanir, model kotasi harcamaz.

Son dogrulanan artifact:

`artifacts/20260608-211210/subagent-run.json`

Son dogrulanan transcript:

`artifacts/20260608-211210/subagent-transcript.ndjson`

## Son Dogrulanan Komutlar

Test:

```bash
dotnet test '\\wsl.localhost\Ubuntu\home\mertb\.openclaw\workspace\acp-net-training-factory\src\acp-net\AcpNetMvp.slnx' --logger 'console;verbosity=minimal'
```

Sonuc:

```text
Acp.Net.UnitTests: 14 passed
Acp.Net.IntegrationTests: 3 passed
```

OpenClaw-style sample:

```bash
dotnet run --project '\\wsl.localhost\Ubuntu\home\mertb\.openclaw\workspace\acp-net-training-factory\src\samples\openclaw-subagent-runner\openclaw-subagent-runner.csproj'
```

Sonuc:

```text
kind=openclaw.subagent.result
sessionId=fake-session-1
stopReason=EndTurn
```

Pack:

```bash
dotnet pack ...Acp.Net.Process.csproj --no-restore --output ...artifacts\packages
dotnet pack ...Acp.Net.Testing.csproj --no-restore --output ...artifacts\packages
```

Sonuc:

```text
Acp.Net.Process.0.1.0-alpha.1.nupkg olustu.
Acp.Net.Testing.0.1.0-alpha.1.nupkg olustu.
```

## Urun Karari

Acp.Net'in OpenClaw icindeki en makul rolu:

> OpenClaw karar ve orkestrasyon katmani olur; Acp.Net ACP-compatible agent/subagent process'lerini guvenilir, test edilebilir ve kanitlanabilir sekilde calistiran runtime substrate olur.

Bugunku spike'lar bu rolu kodla destekledi.

## Sonraki Mantikli Is

Spike 011 onerisi:

**OpenClaw Plugin/Command Integration Probe**

Ama hedef kucuk tutulmali:

1. OpenClaw icinden veya OpenClaw'a yakin bir command wrapper'dan `openclaw-subagent-runner` calistir.
2. Run artifact path'ini yakala.
3. Artifact'i okuyup sonuc/failureKind/preflight bilgisini OpenClaw tarafina ozetle.
4. Hala Gemini/Claude kullanma; once fake agent ile entegrasyon contract'ini sabitle.

Bu gecerse sonraki adim:

- Gemini ile tek kontrollu real-agent run.
- Permission/policy contract.
- Subagent task/result schema.

